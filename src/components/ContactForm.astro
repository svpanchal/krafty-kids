---
const FORM_ENDPOINT = "https://formspree.io/f/mrbdvgvk"; // <-- paste your Formspree endpoint
---

<section id="contact" class="py-20 md:py-24 bg-white">
  <div class="container grid gap-10 md:grid-cols-2 items-start">
    <!-- Left: copy -->
    <div class="max-w-prose">
      <p class="section-kicker">Get in touch</p>
      <h2 class="section-title mt-3 text-3xl md:text-5xl">
        Volunteer, partner, or support a session
      </h2>
      <p class="mt-4 text-slate-600 leading-relaxed">
        Interested in volunteering, partnering with Krafty Kids, or supporting an upcoming event?
        Send us a note and we will get back to you soon.
      </p>
    </div>

    <!-- Right: form -->
    <form
      action={FORM_ENDPOINT}
      method="POST"
      class="rounded-2xl border border-black/5 bg-white p-6 md:p-8 shadow-sm"
    >
      <!-- Honeypot (spam trap). Humans never fill this out -->
      <input type="text" name="_gotcha" class="hidden" tabindex="-1" autocomplete="off" />

      <div class="space-y-4">
        <label class="block">
          <span class="text-sm font-semibold text-slate-900">Name</span>
          <input
            type="text"
            name="name"
            required
            class="mt-1 w-full rounded-xl border border-black/10 bg-white px-3 py-2 text-slate-900 shadow-sm focus:outline-none focus:ring-2 focus:ring-[var(--brand-coral)]/40"
            placeholder="Your name"
          />
        </label>

        <label class="block">
          <span class="text-sm font-semibold text-slate-900">Email</span>
          <input
            type="email"
            name="email"
            required
            class="mt-1 w-full rounded-xl border border-black/10 bg-white px-3 py-2 text-slate-900 shadow-sm focus:outline-none focus:ring-2 focus:ring-[var(--brand-coral)]/40"
            placeholder="you@example.com"
          />
        </label>

        <label class="block">
          <span class="text-sm font-semibold text-slate-900">Message</span>
          <textarea
            name="message"
            rows="5"
            required
            class="mt-1 w-full rounded-xl border border-black/10 bg-white px-3 py-2 text-slate-900 shadow-sm focus:outline-none focus:ring-2 focus:ring-[var(--brand-coral)]/40"
            placeholder="Tell us how you would like to get involved..."
          ></textarea>
        </label>

        <!-- Optional hidden subject line -->
        <input type="hidden" name="_subject" value="New message from Krafty Kids website" />

        <button
          type="submit"
          class="inline-flex items-center justify-center rounded-xl bg-[var(--brand-coral)] px-5 py-2.5 text-white text-sm font-semibold shadow-sm hover:opacity-90 transition w-full md:w-auto"
        >
          Send message
        </button>

        <p class="text-xs text-slate-500 leading-relaxed mt-2">
          By submitting this form, you agree to be contacted by Krafty Kids. We only use your info to respond to your message.
        </p>
      </div>
    </form>
  </div>
</section>
